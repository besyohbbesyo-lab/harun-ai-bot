name: Harun AI Bot CI

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GROQ_API_KEY: gsk_test_dummy_key_for_ci
      TELEGRAM_BOT_TOKEN: "0000000000:AAAAAAAAAAAAAAAAAAAAAAaaaaaaaaaaaaaa"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends ffmpeg portaudio19-dev xvfb

      - name: Install Python dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-asyncio pytest-cov ruff

      - name: Ruff lint check
        continue-on-error: true
        run: ruff check . --output-format=github

      - name: Run tests
        run: xvfb-run pytest tests/ -q --tb=short

      - name: Coverage report
        continue-on-error: true
        run: xvfb-run pytest tests/ -q --cov --cov-report=term-missing --tb=short